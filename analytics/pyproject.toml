[project]
name = "analytics"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dbt-duckdb==1.10.1",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

# Dashboard

This directory owns the analytics presentation layer. Code here should read curated analytical outputs rather than raw collector snapshots, preserving a clean boundary between collection, modeling, and presentation.

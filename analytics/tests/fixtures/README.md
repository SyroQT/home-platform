# Fixtures

This directory owns stable offline fixtures for analytics tests. Store representative raw snapshots, modeled outputs, and other deterministic inputs here so collector, dbt, and dashboard tests can run without live infrastructure access.

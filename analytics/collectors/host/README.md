# Host Collector

This directory owns host-level collector code and documentation. Files here should gather raw VPS state and write immutable snapshots to `analytics/raw/host/{timestamp}.json`, with run metadata written under `analytics/raw/meta/host/{timestamp}.json`.

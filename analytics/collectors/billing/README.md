# Billing Collector

This directory owns billing collector code and documentation. Files here should produce immutable billing snapshots at `analytics/raw/billing/{timestamp}.json`, with run metadata written under `analytics/raw/meta/billing/{timestamp}.json`.

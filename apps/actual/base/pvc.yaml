apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-data
  namespace: apps-actual
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path